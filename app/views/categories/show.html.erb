<p id="notice"><%= notice %></p>
<div class="large-container center-block">
  <h1>
    <strong>Articulos publicados en :</strong>
    <%= @category.title %>
  </h1>

  <p>
    <%= render @category.articles %>
  </p>

  <% if current_user %>
    <%= link_to 'Edit', edit_category_path(@category) %> |
  <% end %>
  <%= link_to 'Back', categories_path %>
</div>